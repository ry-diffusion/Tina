// ============== COMMUNITIES PANEL ==============

import { Palette, ScrollView } from "std-widgets.slint";
import {
    SidebarPanel,
    PanelListItem,
    PanelEmptyState,
} from "../components/sidebar-panel.slint";

export component CommunitiesPanel inherits SidebarPanel {
    title: "Communities";
    search-placeholder: "Search communities";
    show-action-1: false;  // TODO - Missing Implementation: New community
    ScrollView {
        vertical-stretch: 1;
        VerticalLayout {
            alignment: start;
            padding: 16px;
            spacing: 16px;
            
            // TODO - Missing Implementation: Communities list
            
            PanelEmptyState {
                vertical-stretch: 1;
                icon: @image-url("../icons/users.svg");
                title: "TODO - Missing Implementation";
                subtitle: "Communities list will be implemented soon";
            }
        }
    }
}
